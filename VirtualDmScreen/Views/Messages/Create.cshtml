@{
    ViewBag.Title = "Create";
}

@model VirtualDmScreen.Models.Message

@using (Html.BeginForm("Create", "Messages"))
{
    @Html.TextBoxFor(message => message.Text);
    <input type="submit" class="btn btn-default" value="Send" />
}